<div id="show-up">
  <h1>What is tasty out there:</h1>
</div>

<ul id="navlist">
  {{#each products}}
  {{#if ../user}}
  <a href="/user/{{../user._id}}/product/{{ _id }}">
    {{> product_item }}
  </a>
  {{else}}
  {{> product_item }}
  {{/if}}
  {{/each}}
</ul>