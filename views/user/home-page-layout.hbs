<div class="badge badge-primary text-wrap" style="width: 50vw;">
  What's tasty out there?
</div>

<ul id="navlist">
  {{#each products}}
  {{#if ../user}}
  <a href="/user/{{../user._id}}/product/{{ _id }}">
    {{> product_item }}
  </a>
  {{else}}
  {{> product_item }}
  {{/if}}
  {{/each}}
</ul>